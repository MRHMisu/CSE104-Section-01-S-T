#include <stdio.h>

int main()
{
   // place to store data
   int number;
   // show output
   printf("Give a number: ");
   scanf("%d", &number);
   printf("Your given number is : %d \n", number);
   return 0;
}